---
import MainLayout from "@src/layouts/MainLayout.astro";
import TourCard from "@src/components/TourCard.astro";
import Pagination from "@src/components/Pagination.astro";
import { getCollection } from "astro:content";
import HeadingSection from "@src/components/HeadingSection.astro";

const allTours = await getCollection("tours");
const maxPerPage = 8;
const currentPage = 1;
const totalPages = Math.ceil(allTours.length / maxPerPage);
const visibleTours = allTours.slice(0, maxPerPage);
---

<MainLayout title="贵州私家团路线 - 贵州多彩美途旅行社">
  <HeadingSection
    title="贵州私家团路线"
    description="黄果树、荔波小七孔、西江千户苗寨、梵净山、镇远古城，按你的时间和同行人定制。"
    backgroundImage="/img/generated/xijiang-miao-village.webp"
  />
  <section class="relative py-16 md:py-20 overflow-hidden">
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {visibleTours.map((tour) => (
          <TourCard tour={tour} />
        ))}
      </div>

      {totalPages > 1 && (
        <div class="mt-16 flex justify-center">
          <Pagination
            currentPage={currentPage}
            totalPages={totalPages}
            basePath="/tours"
          />
        </div>
      )}
    </div>
  </section>
</MainLayout>
